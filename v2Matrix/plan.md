# v2Matrix — Plan (pointer + decisions)

The full approved build plan lives at
[`C:\Users\DBR2\.claude\plans\ready-to-create-plan-effervescent-sunset.md`](file:///C:/Users/DBR2/.claude/plans/ready-to-create-plan-effervescent-sunset.md)
(user-local; not checked in). This file records the public summary +
decisions that shape the repo.

## Decisions locked in

| Question | Answer |
|---|---|
| TF-1 reference fixture | **Build ourselves** — a small SysMLv2 project generated by a Groovy script that runs inside Cameo and saves `v2Matrix/fixtures/ReferenceProject.mdzip`. |
| MVP launch | **All three** — script console (primary), menu action via a plugin JAR, REST test-harness trigger. |
| FR-15 persistence metaclass | **`ViewUsage` with named feature values** (KerML `FeatureValue` + `LiteralString`). |
| FR-4 default scope | **Heuristic**: exclude any element under a `LibraryPackage` whose `isStandard()` returns true. Confirmed against the real Javadoc during iteration 0. |

## Iteration map

| # | Topic | Commit tag |
|---|---|---|
| 0 | Research, renumber, scaffold, LibraryDetector | `chore(v2Matrix): iteration 0 …` |
| 1 | TF-1 fixture builder + generated `.mdzip` | `feat(v2Matrix): TF-1 …` |
| 2 | `MatrixModel.groovy` (headless query) | `feat(v2Matrix): matrix model …` |
| 3 | `MatrixCanvas.groovy` (Java2D renderer) | `feat(v2Matrix): canvas …` |
| 4 | `MatrixDialog` + `MatrixController` | `feat(v2Matrix): dialog + controller …` |
| 5 | Cell interactions (FR-12) | `feat(v2Matrix): interactions …` |
| 6 | Exports (PNG/SVG/HTML/CSV) | `feat(v2Matrix): exports …` |
| 7 | ViewUsage persistence + auto-load | `feat(v2Matrix): persistence …` |
| 8 | Menu plugin + REST trigger | `feat(v2Matrix): menu + REST …` |
| 9 | `TUTORIAL.md` + main README pointer | `docs(v2Matrix): tutorial …` |

## Deferred open questions

See [requirements.md §6](requirements.md). Non-blocking until the
iteration that needs each one; the planner will ask at that point.

## References

- [requirements.md](requirements.md) — functional + non-functional requirements (source of truth).
- [research-notes.md](research-notes.md) — iteration-0 FQN verification + guide citations.
- Main repo [README](../README.md) — context + install.
- Agent [cameo-api-scripter](../.claude/agents/cameo-api-scripter.md) — tool-call protocol.
